# json2csv

Herramienta CLI para convertir archivos JSON a CSV con controles robustos de excepciones.

> Esqueleto generado con CookieCutter (inciso a). El contenido se completa en los incisos siguientes.
